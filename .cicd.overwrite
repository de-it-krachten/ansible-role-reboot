---

generic:
  role_description: |
    Reboot a server
    By default, only when reboot is pending but it can be forced

# Supported platforms
platforms:
  ubuntu2004:
    supported: true
    ci: true

molecule:
  converge:
    vars:
      reboot_forced: true
